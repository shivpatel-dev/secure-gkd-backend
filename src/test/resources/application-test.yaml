spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
  test:
    database:
      replace: none

secure-gkd:
  security:
    jwt:
      signing-key-base64: ${random.value}${random.value}
